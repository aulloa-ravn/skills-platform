{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd ../.. && npm i -g pnpm && pnpm install && pnpm --filter api prisma:generate && pnpm --filter api prisma:migrate:run && pnpm --filter api build"
  },
  "deploy": {
    "startCommand": "cd apps/api && pnpm start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
