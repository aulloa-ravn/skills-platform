mutation SubmitSkillSuggestion($input: SubmitSkillSuggestionInput!) {
  submitSkillSuggestion(input: $input) {
    suggestionId
    status
    suggestedProficiency
    createdAt
    skill {
      id
      name
      discipline
    }
  }
}
