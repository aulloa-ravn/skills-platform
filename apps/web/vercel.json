{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter web build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
